#include<stdio.h>
{
int l, r;
quickSort(A, 0, arr_size-1);
array*/= 0;
r = arr_size-1; 
while (l < r)
{
if(A[l] + A[r] == sum)
return 1; 
else if(A[l] + A[r] < sum)
l++;
else 
r--;
}    
return 0;
}
 
